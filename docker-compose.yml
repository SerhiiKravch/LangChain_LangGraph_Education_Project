services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: support-agent:local
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
    command: ["uv", "run", "python", "-m", "support_agent.cli"]
